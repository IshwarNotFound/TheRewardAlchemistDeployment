# Core Data Science & Utility Libraries
pandas>=1.3.0
numpy<2.0 # Recommended to avoid v2 issues
scikit-learn>=1.0.0
joblib>=1.0.0
tqdm>=4.60.0

# Web Framework
Flask>=2.0.0
gunicorn>=21.0.0 # Add gunicorn if missing

# NLP / Embeddings
sentence-transformers>=2.2.0

# --- PyTorch & PyTorch Geometric: Installed via build command manually ---
# torch>=1.13.0,<2.0.0
# torch-scatter>=2.1.0
# torch-sparse>=0.6.17
# torch-geometric>=2.3.0

# Optional (for reading .xlsx files)
# openpyxl>=3.0.0
