# -- Base Web App & Utilities --
Flask>=3.0.0
gunicorn>=21.0.0
requests>=2.30.0
joblib>=1.3.0
pandas>=2.1.0
numpy>=1.24.0
scikit-learn>=1.3.0

# -- Sentence Transformers & Dependencies --
sentence-transformers>=2.2.0
transformers>=4.30.0

# -- Optional: Pillow / YAML / tqdm etc.
# Pillow>=9.0.0
# PyYAML>=6.0
# tqdm>=4.60.0

# Do NOT include torch or PyTorch Geometric packages here
# They will be installed manually in the build command
