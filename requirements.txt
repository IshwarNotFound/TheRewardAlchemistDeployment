# requirements.txt for The Reward Alchemist

# ======================================================================
# == !! IMPORTANT INSTALLATION ORDER (Handled by Build Command Now) !! ==
# ======================================================================
# PyTorch and PyTorch Geometric are now installed via the Render Build Command
# to ensure correct versions and dependencies.
# ======================================================================

# Web Framework
Flask>=2.0.0
gunicorn

# Core Data Science & Utility Libraries
pandas>=1.3.0
numpy>=1.21.0
scikit-learn>=1.0.0
joblib>=1.0.0
tqdm>=4.60.0

# NLP / Embeddings
sentence-transformers>=2.2.0

# --- Optional (If reading .xlsx files directly) ---
# openpyxl>=3.0.0

# Note: For exact replication, use 'pip freeze > requirements.txt' in your
# activated virtual environment once all packages are installed and working.
