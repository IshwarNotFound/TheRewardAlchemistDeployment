# -- Base Web App & Utilities --
Flask>=3.0.0
gunicorn>=21.0.0 # Or specific version like 23.0.0 from your log
requests>=2.30.0
joblib>=1.3.0
pandas>=2.1.0 # Or specific version like 2.2.3 from your log
numpy>=1.24.0
scikit-learn>=1.3.0 # Or specific version like 1.6.1 from your log

# -- PyTorch Core (CPU Version) --
# Force pip to look for CPU-specific wheels first for torch packages
--index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu  # Using a recent stable CPU version
# torchvision==0.17.2+cpu # Add if needed by your models/transforms
# torchaudio==2.2.2+cpu  # Add if needed by your models/transforms

# -- PyTorch Geometric & Dependencies (CPU) --
# Let pip find compatible CPU versions based on torch==2.2.2+cpu from the index above
torch-scatter
torch-sparse
torch-cluster # Often needed
torch-spline-conv # Sometimes needed
torch-geometric

# -- Sentence Transformers & Dependencies --
sentence-transformers>=2.2.0 # Or specific version like 4.1.0 from your log
transformers>=4.30.0 # sentence-transformers depends on this
# huggingface-hub # Often installed as a dependency of transformers/sentence-transformers

# -- Other potential dependencies seen in logs/imports --
# Add any other specific libraries your code imports if not covered above
# Examples (adjust versions if needed):
# Pillow>=9.0.0
# PyYAML>=6.0
# tqdm>=4.60.0
# Jinja2>=3.1.0 # Usually installed with Flask
# Werkzeug>=3.0.0 # Usually installed with Flask
# itsdangerous>=2.0.0 # Usually installed with Flask
# click>=8.0.0 # Usually installed with Flask
# MarkupSafe>=2.0.0 # Usually installed with Jinja2
